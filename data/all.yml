apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: backstage-app-root-location
  description: Location of all git controlled resources
spec:
  targets:
    - ./../catalog-info.yaml
    - ./all-groups.yml
